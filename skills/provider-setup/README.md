# provider-setup

Configures `openclaude` to route through an OpenAI-compatible provider
(Groq, Together, OpenRouter, vLLM, Ollama, etc.) — base URL, key, model,
verify request, and troubleshooting by HTTP status. Example:
"Point openclaude at Groq".
