# frontend-implementation

Builds UI components and features that match the project's existing
patterns — state management, styling, forms, routing, primitives.
Implements loading/empty/error states alongside the populated one and
wires the result into the right parent. Example: "Add a settings panel".
