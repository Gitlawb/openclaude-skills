# pr-description-writer

Writes pull request bodies that match the repo's existing convention —
typically Problem / Fix / Verification, but follows whatever shape recent
merged PRs use. Verification sections are concrete commands with expected
output, not "test it works". Example: "write a PR description for this
branch".
