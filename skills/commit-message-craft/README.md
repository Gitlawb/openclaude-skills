# commit-message-craft

Inspects staged changes and writes a commit message that matches the repo's
existing convention — conventional commits, ticket-prefixed, or plain
imperative. Proposes splitting multi-purpose changes into separate commits
with their own messages. Example: "commit my staged changes".
