# database-review

Reviews migrations, schema changes, and queries. Checks lock duration,
rolling-deploy compatibility, index-to-query matching, plan shape,
backfill hazards, and data integrity. Example: "Is this ALTER TABLE
safe to deploy?".
