# release-maintainer

Prepares releases. Lists PRs since the last tag, groups breaking /
features / fixes / internal, picks the semver bump from what's in,
writes user-facing notes with migration steps, updates the changelog,
and produces the tag commands for the maintainer to run.
